require "spec_helper"

describe CacheSpec::Caches::PageCaching do
  context "#cache_page" do
    it "matches cached page with specific path"
    it "matches cached page content with specific path"
  end

  context "#expire_page" do
    it "expires page with a specific path"
  end
end
